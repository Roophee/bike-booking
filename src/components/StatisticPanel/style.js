import styled from 'styled-components';

const StyledStatisticPanel = styled.div`
  padding-left: 10px;
  & * {
    margin: 5px 0;
    font-family: Saira;
  }

  & h3 {
    font-size: 18px;
    line-height: 28px;
    text-transform: uppercase;
  }

  & p {
    font-size: 14px;
    line-height: 22px;
  }
`;
export default StyledStatisticPanel;
