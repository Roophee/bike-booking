import styled from 'styled-components';

const StyledDeleteBikeIcon = styled.div`
  //font-size: ${props => props.fontSize};
  cursor: pointer;
  user-drag: none;
  user-select: none;
`;

export default StyledDeleteBikeIcon;
